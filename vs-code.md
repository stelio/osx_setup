# How to Launch VS Code from the Command Line
On macOS, Open VS Code.

1. Open the Command Palette (⇧⌘P or F1).
2. Type "shell command" and select the command: Shell Command: Install 'code' command in PATH.
3. Restart your terminal for the changes to take effect.
